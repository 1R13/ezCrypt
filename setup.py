from setuptools import setup
import os

setup(
	name='ezCrypt',
	version='0.1.0',
	author='1r13',
	url='#',
	description='simple binary encryption with fernet',
	license='free4all',
	install_requires=['cryptography'],
	packages=['ezCrypt']
)
